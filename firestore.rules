rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 🔓 OPEN DEVELOPMENT RULES
    // Allows anyone with your project ID to read/write to your database.
    // Use this ONLY for initial setup and migration.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
